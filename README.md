# swig-moment


```
# Install with
npm install swig-moment

# Use
const swig  = require('swig');
const swig_moment = require('swig-moment')('fi'); // or just (); -> en
swig.setFilter('mdate', swig_moment.date);

```
